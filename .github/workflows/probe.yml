name: probe
on:
  push:
    branches: [claude/tax-savings-retirement-simulator-bxeilh]
  workflow_dispatch:
jobs:
  plain:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Actions 자체는 동작함"
  needs-pages-perm:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pages: write
      id-token: write
    steps:
      - run: echo "pages:write 권한 획득 성공"
